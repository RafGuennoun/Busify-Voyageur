

import 'package:flutter/material.dart';

class ErrorLogo extends StatelessWidget {
  const ErrorLogo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Text("ayemma Error"),
    );
  }
}